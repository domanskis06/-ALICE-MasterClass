export const AppConfig = {
  production: false,
  environment: 'DEV',
  apiUrl: '',
  // Jeśli true - frontend użyje MockApiService zamiast rzeczywistego ApiService
  teacherMode: true,
  version: '0.0.0'
};
